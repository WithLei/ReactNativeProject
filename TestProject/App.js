// 'use strict';
import React, {
  Component
} from 'react';

var LoginView = require('./components/loginView');

export default class TestProject extends Component {
  render() {
    return ( <
      LoginView / >
    );
  }
}